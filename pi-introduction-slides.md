# 🚀 Pi 编程助手 - 产品介绍

**一个极简、灵活、可扩展的终端编程工具**

---

## 📋 目录

1. [什么是 Pi？](#什么是-pi)
2. [安装与配置](#安装与配置)
3. [核心能力](#核心能力)
4. [用户界面](#用户界面)
5. [常见使用方式](#常见使用方式)
6. [经典使用案例](#经典使用案例)
7. [目标用户群体](#目标用户群体)
8. [快速上手](#快速上手)

---

# 🎯 什么是 Pi？

## 核心理念

> **"Adapt pi to your workflows, not the other way around"**
> 
> 让 pi 适应你的工作流程，而非相反

## 关键特点

| 特点 | 描述 |
|------|------|
| 🔧 **极简核心** | 内置基础工具（读、写、编辑、执行），无多余功能 |
| 🧩 **高度可扩展** | 通过 Extensions、Skills、Themes 自由定制 |
| 📦 **Pi Packages** | npm/git 共享插件生态 |
| 🖥️ **四种运行模式** | 交互式、打印输出、JSON、RPC |

## 与其他工具的对比

```
┌─────────────┬───────────┬───────────┬───────────┐
│    特性     │    Pi     │ Claude Code│  GitHub Copilot │
├─────────────┼───────────┼───────────┼───────────┤
│ 子代理      │ ❌ 按需构建 │  ✅ 内置   │  ✅ 内置    │
│ Plan 模式   │ ❌ 按需构建 │  ✅ 内置   │  ✅ 内置    │
│ 权限弹窗    │ ❌ 按需构建 │  ✅ 内置   │  ✅ 内置    │
│ 内置 Todo   │ ❌ 按需构建 │  ✅ 内置   │  ✅ 内置    │
│ MCP 集成    │ ❌ 按需构建 │  ✅ 内置   │  ❌        │
│ 扩展性      │  ⭐⭐⭐⭐⭐  │   ⭐⭐⭐     │   ⭐⭐      │
│ 核心体积    │   极小     │    中等    │   中等     │
└─────────────┴───────────┴───────────┴───────────┘
```

---

# 📥 安装与配置

## 系统要求

- **Node.js**: v18+ (推荐 v20+)
- **包管理器**: npm, yarn, pnpm, bun
- **操作系统**: macOS, Linux, Windows, Termux (Android)

## 安装步骤

```bash
# 1. 全局安装
npm install -g @mariozechner/pi-coding-agent

# 2. 验证安装
pi --version

# 3. 认证 (二选一)
# 方式A: API Key
export ANTHROPIC_API_KEY=sk-ant-...

# 方式B: 订阅登录
pi
/login  # 交互式选择提供商
```

## 配置目录结构

```
~/.pi/agent/
├── extensions/          # TypeScript 扩展
├── skills/              # 技能包
├── prompts/             # 提示模板
├── themes/              # 主题文件
├── sessions/            # 会话存储
├── settings.json        # 全局设置
├── AGENTS.md            # 全局上下文
└── SYSTEM.md            # 自定义系统提示
```

## 配置文件示例

```json
// ~/.pi/agent/settings.json
{
  "theme": "dark",
  "thinkingLevel": "medium",
  "sessionDir": "~/.pi/agent/sessions",
  "packages": [
    "npm:@foo/pi-tools"
  ],
  "extensions": [],
  "skills": [],
  "prompts": []
}
```

---

# ⚡ 核心能力

## 1. 内置工具

| 工具 | 命令 | 功能 |
|------|------|------|
| 📖 `read` | 内置 | 读取文件内容，支持大文件分片 |
| ✏️ `write` | 内置 | 创建或覆盖文件 |
| 🔄 `edit` | 内置 | 精确文本替换编辑 |
| 💻 `bash` | 内置 | 执行 shell 命令 |
| 🔍 `grep` | 内置 | 文本搜索 |
| 📁 `find` | 内置 | 文件查找 |
| 📄 `ls` | 内置 | 目录列表 |

## 2. 会话管理

```
会话功能
├── 自动保存到 ~/.pi/agent/sessions/
├── 树形结构 - 支持分支和回溯
├── 对话压缩 - 上下文溢出时自动总结
├── 分支 (Branch) - 探索不同方案
└── 恢复 (Resume) - 从任意历史点继续
```

## 3. 多模型支持

**支持的订阅服务：**
- ✅ Anthropic Claude (Pro/Max)
- ✅ OpenAI ChatGPT (Plus/Pro/Codex)
- ✅ GitHub Copilot
- ✅ Google Gemini CLI
- ✅ Google Antigravity

**API Key 模式：**
- Anthropic, OpenAI, Azure OpenAI
- Google Gemini, Vertex
- Amazon Bedrock, Mistral
- Groq, Cerebras, xAI
- OpenRouter, Hugging Face, Kimi

## 4. 可扩展性架构

```
          ┌─────────────────────────────┐
          │        Pi Core              │
          │  ┌─────────────────────┐   │
          │  │   内置工具           │   │
          │  │ read, write, edit   │   │
          │  └─────────────────────┘   │
          │  ┌─────────────────────┐   │
          │  │   会话管理           │   │
          │  │  树形结构, 压缩      │   │
          │  └─────────────────────┘   │
          └─────────────────────────────┘
                       │
    ┌──────────────────┼──────────────────┐
    │                  │                  │
    ▼                  ▼                  ▼
┌────────┐       ┌──────────┐       ┌──────────┐
│Extension│       │  Skill   │       │  Prompt  │
│TypeScript│       │ Agent标准│       │ Markdown │
└────────┘       └──────────┘       └──────────┘
```

---

# 🖥️ 用户界面

## 交互模式布局

```
┌────────────────────────────────────────────────────────────┐
│  [Startup Header: 快捷键 / 已加载的技能 / 扩展]            │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  [Messages Area]                                           │
│  ├── 你的消息                                               │
│  ├── AI 响应                                                │
│  ├── 工具调用和结果                                         │
│  ├── 通知和错误                                             │
│  └── 扩展 UI                                               │
│                                                            │
├────────────────────────────────────────────────────────────┤
│  [Editor - 输入区域]                                       │
│  边框颜色表示思考层级                                       │
├────────────────────────────────────────────────────────────┤
│  [Footer: 工作目录 | 会话名 | Token 使用 | 费用 | 模型]     │
└────────────────────────────────────────────────────────────┘
```

## 核心快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+C` | 清空编辑器 / 退出 |
| `Escape` | 取消/中止 |
| `Ctrl+L` | 切换模型 |
| `Ctrl+P` | 循环切换可用模型 |
| `Shift+Tab` | 切换思考层级 |
| `Ctrl+O` | 折叠/展开工具输出 |
| `@` | 引用项目文件 |
| `!command` | 执行并发送给 LLM |
| `!!command` | 仅执行 |

## 常用命令

| 命令 | 描述 |
|------|------|
| `/login` | OAuth 认证 |
| `/model` | 切换模型 |
| `/settings` | 打开设置 |
| `/new` | 新建会话 |
| `/resume` | 浏览并选择历史会话 |
| `/tree` | 会话树导航 |
| `/fork` | 从当前分支创建新会话 |
| `/compact` | 手动压缩上下文 |
| `/export` | 导出会话为 HTML |

---

# 🔧 常见使用方式

## 1. 交互式编程

```bash
# 基本交互
pi

# 继续上次会话
pi -c

# 浏览选择历史会话
pi -r

# 指定模型
pi --model anthropic/claude-sonnet-4

# 高思考层级
pi --model sonnet:high "解决这个复杂问题"
```

## 2. 非交互式使用

```bash
# 打印输出模式
pi -p "列出 src 目录下所有 TypeScript 文件"

# 管道输入
cat README.md | pi -p "总结这段文本"

# 带文件引用
pi @code.ts @test.ts "Review these files"

# 只读模式 (限制工具)
pi --tools read,grep,find,ls -p "Review the code"
```

## 3. 嵌入集成

```typescript
// SDK 模式
import { createAgentSession } from "@mariozechner/pi-coding-agent";

const { session } = await createAgentSession({
  // 配置选项
});

await session.prompt("在当前目录下有哪些文件？");
```

```bash
# RPC 模式
pi --mode rpc

# JSON 输出模式
pi --mode json
```

## 4. 会话分支与探索

```
会话树示例:

                    [根节点]
                       │
        ┌──────────────┼──────────────┐
        ▼              ▼              ▼
     [方案A]        [方案B]        [方案C]
        │              │              │
        ▼              ▼              ▼
   [细化A1]       [细化B1]       [细化C1]
        │              │              │
        ▼              ▼              ▼
    [采纳✓]       [回退]         [评估中]
    
使用 /tree 自由切换分支
使用 /fork 创建新会话
```

---

# 💡 经典使用案例

## 案例 1: Vibe Coding 🎵

**场景：** 用自然语言描述想法，AI 帮你实现

```
用户:
┌─────────────────────────────────────────────────────────────┐
│ "创建一个好看的 React 登录页面，包含:                        │
│  - 邮箱和密码输入框                                         │
│  - 记住我复选框                                             │
│  - 登录按钮                                                 │
│  - 渐变背景                                                 │
│  - 表单验证                                                 │
│  使用 Tailwind CSS"                                        │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
                        [Pi 读取项目结构]
                            │
                            ▼
                        [Pi 创建组件文件]
                            │
                            ▼
                        [Pi 启动开发服务器]
                            │
                            ▼
                        [Pi 展示结果]
```

**Pi 优势：**
- ✅ 自然语言理解能力强
- ✅ 可以直接创建/编辑文件
- ✅ 实时执行命令验证
- ✅ 支持图片输入（截图反馈）

## 案例 2: 需求分析与设计 📐

**场景：** 产品经理描述需求，AI 转化为技术方案

```
产品经理 (PM):
┌─────────────────────────────────────────────────────────────┐
│ "我们需要做一个用户反馈系统:                                 │
│  - 用户可以提交文字反馈                                      │
│  - 可以上传截图                                             │
│  - 支持匿名提交                                             │
│  - 后台管理员可以看到所有反馈                                │
│  - 可以标记状态: 待处理/处理中/已解决                        │
│  - 需要邮件通知管理员有新反馈"                               │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │   Pi 分析需求  │
                    │  设计技术方案  │
                    └───────────────┘
                            │
          ┌─────────────────┼─────────────────┐
          ▼                 ▼                 ▼
    ┌──────────┐     ┌──────────┐     ┌──────────┐
    │  API 设计 │     │ 数据库Schema│     │ 前端组件  │
    │ routes.ts│     │ schema.prisma│  │ FeedbackForm.tsx│
    └──────────┘     └──────────┘     └──────────┘
```

**Pi 优势：**
- ✅ 理解业务逻辑
- ✅ 生成完整项目结构
- ✅ 写出可直接运行的代码
- ✅ 节省开发时间 60%+

## 案例 3: 功能构思与原型 🔮

**场景：** 头脑风暴，探索多种实现方案

```
用户:
┌─────────────────────────────────────────────────────────────┐
│ "我想实现实时协作编辑功能，有什么方案？                        │
│  - 要考虑冲突解决                                            │
│  - 性能要支持 100 人同时编辑                                 │
│  - 考虑离线支持"                                            │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │   Pi 分析     │
                    │ 多种技术方案   │
                    └───────────────┘
                            │
          ┌─────────────────┼─────────────────┐
          ▼                 ▼                 ▼
    ┌──────────┐     ┌──────────┐     ┌──────────┐
    │方案A:OT   │     │方案B:CRDT│     │方案C:WebRTC│
    │(Operational│   │(Conflict-│     │(点对点)  │
    │Transform) │     │free Repl.│     │          │
    └──────────┘     │ DataType)│     └──────────┘
                     └──────────┘
```

**Pi 优势：**
- ✅ 快速探索多种方案
- ✅ `/fork` 分支对比不同实现
- ✅ 生成对比分析报告
- ✅ 节省研究时间

## 案例 4: 代码重构 🛠️

**场景：** 改进现有代码质量

```bash
# 启动 Pi 并指定只读模式审查
pi --tools read,grep,find,ls -p "Review and refactor this codebase"

# 或者交互式重构
pi

# 告诉 Pi:
# "分析 src 目录下的代码，找出:
#  - 重复代码
#  - 潜在的 bug
#  - 性能问题
#  - 安全漏洞
# 然后给出重构建议"
```

## 案例 5: 学习新技术 📚

**场景：** 快速上手新框架/库

```
用户:
┌─────────────────────────────────────────────────────────────┐
│ "我需要用 Next.js 14 App Router 创建一个博客，              │
│  - 使用 TypeScript                                          │
│  - 使用 Prisma + PostgreSQL                                 │
│  - 实现 MDX 博客文章渲染                                    │
│  - 添加评论区                                               │
│  请从头开始解释并实现"                                      │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │   Pi 逐步指导  │
                    │ 边解释边实现   │
                    └───────────────┘
```

---

# 👥 目标用户群体

## 前端开发者

```
使用场景:
├── 🎨 快速创建 UI 组件
├── ⚡ 样式调试和优化
├── 🔧 React/Vue/Angular 开发
├── 📦 组件库搭建
└── 🧪 自动化测试编写

Pi 特别擅长:
├── 读取并理解 JSX/Vue 文件
├── 编辑样式文件
├── 启动开发服务器预览
└── 浏览器自动化测试
```

## 后端开发者

```
使用场景:
├── 🏗️ API 设计实现
├── 🗄️ 数据库设计
├── 🔒 安全审计
├── ⚙️ DevOps 配置
└── 📊 性能优化

Pi 特别擅长:
├── 读写各类后端语言代码
├── 数据库迁移脚本
├── Docker/K8s 配置
├── CI/CD 流程编写
└── SQL 查询优化
```

## 产品经理

```
使用场景:
├── 📝 需求文档编写
├── 🎨 原型设计辅助
├── 📊 数据分析
├── 🔍 竞品调研
└── 🤝 团队沟通

Pi 特别擅长:
├── 自然语言处理需求
├── 生成技术方案草稿
├── 图表和文档创建
├── 网络研究和总结
└── 格式转换和整理

推荐技能:
├── /skill:web-search (网页搜索)
├── /skill:crawl (网站爬取)
└── /skill:pdf-tools (PDF处理)
```

## 团队协作建议

| 用户类型 | 推荐配置 | 使用模式 |
|----------|----------|----------|
| 前端开发者 | React/Vue 技能 + 浏览器自动化 | 交互式 |
| 后端开发者 | 数据库技能 + 安全检查 | 交互式 + 批量 |
| 产品经理 | 搜索技能 + 文档模板 | 交互式 |
| 全栈 | 完整技能包 | 混合模式 |

---

# 🚀 快速上手

## 第一步：安装

```bash
npm install -g @mariozechner/pi-coding-agent
```

## 第二步：认证

```bash
# 方式 1: API Key
export ANTHROPIC_API_KEY=sk-ant-...

# 方式 2: 交互登录
pi
/login
```

## 第三步：开始对话

```bash
# 基础使用
pi

# 试试这些:
pi "你好，请介绍你自己"

# 读取当前项目
pi "这个项目是做什么的？"

# 创建新功能
pi "帮我创建一个用户登录页面"
```

## 第四步：探索高级功能

```bash
# 切换模型
/model

# 查看帮助
/hotkeys

# 创建技能
mkdir -p ~/.pi/agent/skills/web-search
# 然后让 Pi 帮你写 SKILL.md

# 安装主题
pi install npm:@foo/pi-theme
```

---

# 📚 资源链接

| 资源 | 链接 |
|------|------|
| 官网 | https://pi.dev |
| npm | https://www.npmjs.com/package/@mariozechner/pi-coding-agent |
| GitHub | https://github.com/badlogic/pi-mono |
| Discord | https://discord.com/invite/3cU7Bz4UPx |

## 社区技能包

- **Anthropic Skills**: https://github.com/anthropics/skills
  - 文档处理 (docx, pdf, pptx, xlsx)
  - Web 开发
  
- **Pi Skills**: https://github.com/badlogic/pi-skills
  - 网页搜索
  - 浏览器自动化
  - Google APIs
  - 语音转录

---

# ✅ 总结

## Pi 的核心价值

| 价值 | 说明 |
|------|------|
| 🎯 **精准** | 专注代码任务，不过度设计 |
| 🔧 **灵活** | 按需扩展，无限可能 |
| ⚡ **高效** | 减少重复工作，提升生产力 |
| 🤝 **协作** | 适配多种工作流程 |
| 📦 **生态** | npm/git 共享，无 vendor lock-in |

## 开始使用 Pi

```bash
# 一行命令，开始你的 AI 编程之旅
npm install -g @mariozechner/pi-coding-agent && pi
```

---

**谢谢观看！** 🎉

有任何问题，欢迎在 Discord 社区交流！
